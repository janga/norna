# Reference

## Configuration files

`config.yaml` contains the public URL and technical choices. `theme.yaml`
selects a preset and optional visual overrides. `sitewide-content.yaml`
contains shared elements such as banners and the footer.

## Page files

Each page's `content.md` supplies its title and headings. Local images belong
in the adjacent `images/` directory.
