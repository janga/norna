# GitHub Pages

## Public address

Set `url` in `site/config.yaml` to the final public address, including the
repository path when it is part of that address.

## Deployment

Choose GitHub Actions as the repository's Pages source. Push the generated
workflow together with the site files to trigger the build and deployment.
