# Publishing

## Build before publishing

Run `npm run norna:build` to check the source and build the static site.

## Choose a destination

Serve the generated `dist/` directory on a static host. For GitHub Pages,
the generated project's workflow builds the site after a push.
