# Windows

## Prerequisites

Install Node.js 22.12 or later. For raster images, install ImageMagick too and
make sure its commands are available in your terminal.

## Local preview

Run `npm run norna:dev` in PowerShell from the site project's directory and
open the printed address in your browser.
