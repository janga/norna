# macOS

## Prerequisites

Install Node.js 22.12 or later. For raster images, install ImageMagick too.

## Local preview

Run `npm run norna:dev` from the site project's directory and open the printed
address in your browser.
