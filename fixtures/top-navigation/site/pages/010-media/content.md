# Media blocks

## Image stacks

Image stacks keep related images and captions together in the reading flow.

This fixture uses enough prose to verify direct links, sticky navigation offsets and browser history without depending on a public demonstration site. The text also gives later sections enough space to align with the sticky navigation instead of stopping at the bottom edge of the page.

## Carousels

Carousels present a sequence in one stable media area.

Section navigation should move this heading directly below the sticky navigation. Repeated navigation must keep the URL, current-section state and keyboard focus synchronized with the visible heading.

The remaining content gives the browser room to place the heading at the requested scroll position on both compact and wide viewports.

## Cards

Cards group short, related pieces of content.

Moving backward and forward through section links should restore each section without changing the active page or losing the navigation context.

This paragraph deliberately extends the fixture so the preceding heading is not constrained by the bottom of the document.

## Captions

Captions remain associated with the media they explain.

The final section provides a stable destination for rapid consecutive navigation and history tests.

Additional fixture text keeps the page long enough for consistent measurements at desktop and mobile viewport heights.
