# Themes

This feature demo compares Norna's complete built-in theme presets. Every route
uses one preset without overrides, so differences in layout, typography,
palette, surfaces and image presentation come from the preset itself.

Run it from the repository root:

```sh
node bin/norna.mjs --site-dir examples/feature-demos/theme-presets/site dev:local
```

The dog photos are CC0 images by Bicanski, sourced from Pixnio. Attribution is
included in the image captions even though the licence does not require it.
